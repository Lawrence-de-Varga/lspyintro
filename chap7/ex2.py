# tuples are immutable so this cannopt be changed
# The tuple could be transformed into a list, modified and then made into a tuple again, 
# but it would not be the same tuple.

stuff = ('hello', 'world', 'bye', 'now')


